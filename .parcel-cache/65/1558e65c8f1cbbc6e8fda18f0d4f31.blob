module.exports="#define GLSLIFY 1\n// varying vec2 vUv;\nvoid main() {\n  // vUv = uv;\n  vec3 newposition = position;\n  newposition.y += 0.1*sin(newposition.x*10.);\n\n  gl_Position = projectionMatrix * modelViewMatrix * vec4( newposition, 1.0 );\n}";